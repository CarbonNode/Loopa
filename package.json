{
  "name": "loopa",
  "version": "0.1.0",
  "private": true,
  "description": "Self-hosted catalog for funny videos and GIFs — Plex-shaped, AI-tagged, instantly searchable.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c cyan,magenta \"npm:dev:api\" \"npm:dev:web\"",
    "dev:api": "npm --workspace server run dev",
    "dev:web": "npm --workspace web run dev",
    "_comment_build": "Only the web app builds. The server runs TypeScript directly via Node 22 type stripping, so it has no build step by design.",
    "build": "npm --workspace web run build",
    "build:web": "npm --workspace web run build",
    "start": "npm --workspace server run start",
    "typecheck": "npm --workspace server run typecheck && npm --workspace web run typecheck"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
